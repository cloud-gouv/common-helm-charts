# Default values for matrix-receiver.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

receiver:
  matrix:
    homeserverUrl: ''
    userId: ''
    accessToken: secret
    # define short names for Matrix room ID
    roomMapping:
      default: ''
  computedValues:
    - values: # always set 'color' to 'yellow'
        color: yellow
    - values: # set 'color' to 'orange' when alert label 'severity' is 'warning'
        color: orange
      when-matching-labels:
        severity: warning
    - values: # set 'color' to 'red' when alert label 'severity' is 'critical'
        color: red
      when-matching-labels:
        severity: critical
    - values: # set 'color' to 'green' when alert status is 'resolved'
        color: green
      when-matching-status: resolved
  firingTemplate: |
    <p>
      <strong><font color="{{ .ComputedValues.color }}">{{ .Alert.Status | ToUpper }}</font></strong><br>
      <b>Alert:</b> {{ if .Alert.Annotations.summary }}{{ .Alert.Annotations.summary }}{{ else if .Alert.Annotations.message }}{{ .Alert.Annotations.message }}{{ end }}{{ if .Alert.Labels.severity }} - <b>{{ .Alert.Labels.severity }}</b>{{ end }}<br>
      <b>Description:</b> {{ .Alert.Annotations.description }}<br>
      <b>Details:</b><br>
      <table border="0">
        <tr>
        <th>Label</th>
        <th>Value</th>
        </tr>
      {{ range .Alert.Labels.SortedPairs }}
        <tr>
          <td><i>{{ .Name }}</i></td>
          <td>{{ .Value }}</td>
        </tr>
      {{ end }}
      </table>
    </p>
  resolvedTemplate: |
    <strong><font color="{{ .ComputedValues.color }}">{{ .Alert.Status | ToUpper }}</font></strong><br>
    {{ if .Alert.Labels.cluster }}In <b>{{ .Alert.Labels.cluster }}</b>:{{ end }}
    {{ if .Alert.Labels.name }}{{ .Alert.Labels.name }}
    {{ else if .Alert.Annotations.description }}{{ .Alert.Annotations.description }}
    {{ else if .Alert.Annotations.summary }}{{ .Alert.Annotations.summary }}
    {{ end }}

# This sets the container image more information can be found here: https://kubernetes.io/docs/concepts/containers/images/
image:
  repository: metio/matrix-alertmanager-receiver
  # This sets the pull policy for images.
  pullPolicy: IfNotPresent
  # Overrides the image tag whose default is the chart appVersion.
  # renovate: datasource=github-releases depName=metio/matrix-alertmanager-receiver versioning=helm
  tag: "2025.4.23"

# This is for the secretes for pulling an image from a private repository more information can be found here: https://kubernetes.io/docs/tasks/configure-pod-container/pull-image-private-registry/
imagePullSecrets: []
# This is to override the chart name.
nameOverride: ""
fullnameOverride: ""

#This section builds out the service account more information can be found here: https://kubernetes.io/docs/concepts/security/service-accounts/
serviceAccount:
  # Specifies whether a service account should be created
  create: true
  # Automatically mount a ServiceAccount's API credentials?
  automount: true
  # Annotations to add to the service account
  annotations: {}
  # The name of the service account to use.
  # If not set and create is true, a name is generated using the fullname template
  name: ""

# This is for setting Kubernetes Annotations to a Pod.
# For more information checkout: https://kubernetes.io/docs/concepts/overview/working-with-objects/annotations/
podAnnotations: {}
# This is for setting Kubernetes Labels to a Pod.
# For more information checkout: https://kubernetes.io/docs/concepts/overview/working-with-objects/labels/
podLabels: {}

podSecurityContext: {}
  # fsGroup: 2000

securityContext: {}
  # capabilities:
  #   drop:
  #   - ALL
  # readOnlyRootFilesystem: true
  # runAsNonRoot: true
  # runAsUser: 1000

# This is for setting up a service more information can be found here: https://kubernetes.io/docs/concepts/services-networking/service/
service:
  # This sets the service type more information can be found here: https://kubernetes.io/docs/concepts/services-networking/service/#publishing-services-service-types
  type: ClusterIP
  # This sets the ports more information can be found here: https://kubernetes.io/docs/concepts/services-networking/service/#field-spec-ports
  port: 3000

# This is to setup the liveness and readiness probes more information can be found here: https://kubernetes.io/docs/tasks/configure-pod-container/configure-liveness-readiness-startup-probes/
livenessProbe:
  httpGet:
    path: /metrics
    port: http
readinessProbe:
  httpGet:
    path: /metrics
    port: http

nodeSelector: {}

tolerations: []

affinity: {}

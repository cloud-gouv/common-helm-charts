╔══════════════════════════════════════════════════════════════╗
║         Envoy Gateway Routes — Installed!                    ║
╚══════════════════════════════════════════════════════════════╝

Release  : {{ .Release.Name }}
Namespace: {{ .Release.Namespace }}
Chart    : {{ .Chart.Name }}-{{ .Chart.Version }}

Resources deployed:
  GatewayClass     : {{ len .Values.gatewayClasses }}
  Gateway          : {{ len .Values.gateways }}
  EnvoyProxy       : {{ len .Values.envoyProxies }}

Verify status:
  kubectl get gatewayclass
  kubectl get gateway -n {{ .Release.Namespace }}
  kubectl describe gateway -n {{ .Release.Namespace }}
  kubectl get envoyproxy -A

Docs: https://gateway.envoyproxy.io/docs/

apiVersion: v2
name: envoy-gateway-infra
description: |
  Helm chart for managing Envoy Gateway API routing resources:
  Gateway, GatewayClass.
type: application
version: 1.0.0
appVersion: "1.2.0"
keywords:
  - envoy
  - gateway
  - GatewayClass
maintainers:
  - name: platform-team
    email: platform@example.com
annotations:
  category: Networking

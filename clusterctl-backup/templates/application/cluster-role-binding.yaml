apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: clusterctl-read
subjects:
  - kind: ServiceAccount
    name: clusterctl-backup
    namespace: {{ .Values.namespace }} 
roleRef:
  kind: ClusterRole
  name: clusterctl-read
  apiGroup: rbac.authorization.k8s.io